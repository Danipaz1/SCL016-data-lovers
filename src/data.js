// Hacer funcion obtenerCampeones 
export const obtenerCampeones = (lol, tag) => {

  console.log(lol);

  for (champion in lol) {
    champion.tags
  }


  return 'example';
};

export const anotherExample = () => {
  return 'OMG';
}